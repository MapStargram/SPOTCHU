# 앱(Next.js)과 무관한 도구·세션·데이터 디렉터리는 배포 업로드에서 제외.
# .claude/worktrees(세션 git 워크트리)에 dangling 파일이 있으면 lstat ENOENT로 배포가 깨진다.
# ⚠️ 루트 앵커(/) 필수 — 앵커 없는 `research`는 scripts/research 같은 하위 동명 디렉터리까지
#    제외해 빌드가 깨진다(scripts/research-orchestrator.ts → scripts/research/config 미해결).
/.claude
/.agent
/research
/meta-prompt
