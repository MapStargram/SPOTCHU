# design.md — SPOTCHU 디자인 시스템 (v0)

> 상위 원천: [`prd.md`](prd.md) §29·§30. 모바일 우선 · 지도/사진 중심. 이 문서는 초안이며 Phase 1 UI 착수 시 실제 토큰으로 확정한다. 접근성은 타협하지 않는다(WCAG AA 지향).

## 1. 디자인 원칙
- **위치가 주인공**: 사진은 아름답되, 화면은 "정확히 어디서 어떻게 찍는가"를 최우선으로 전달한다.
- **모바일 한 손 조작**: 하단 내비 4탭 + 엄지 영역 액션(저장·인증). 지도⇄피드 전환은 즉각.
- **정보 밀도 대신 명료함**: 스팟 카드/상세는 핵심(구도·검증상태·거리)부터.
- **AI 슬롭 금지**: 과도한 그라데이션/이모지 남발/불필요한 애니메이션 배제. 조용하고 실용적으로.

## 2. 컬러 토큰 (역할 기반)
라이트 기준. 다크는 역할 유지·값만 스왑(Phase 1 확정). CSS 변수로 관리.
```
--bg            : #FFFFFF
--surface       : #F6F7F9
--text          : #14181F
--text-muted    : #5B6472
--border        : #E3E7EC
--primary       : #2F6BFF   /* 액션·링크 */
--primary-ink   : #FFFFFF
--accent        : #FF5A5F   /* 저장/좋아요 강조 */
```

### 검증 상태 색 (지도 핀 = 색으로 구분, 항상 라벨/아이콘 병기)
```
--v-official    : #2F6BFF   /* OFFICIAL 공식 인증 (체크 아이콘) */
--v-verified    : #1FA971   /* USER_VERIFIED 사용자 검증 (사람 아이콘) */
--v-reported    : #F5A524   /* USER_REPORTED 사용자 제보 (점선 테두리) */
--v-estimated   : #8B5CF6   /* ESTIMATED 위치 추정 (물음표·점선) */
```
> 색만으로 상태를 전달하지 않는다. 핀/뱃지에 아이콘 + 텍스트 라벨을 함께 쓴다(색각 접근성).

## 3. 타이포그래피
- 한글/일문 혼용 대응: 시스템 산세리프 스택 우선(`Pretendard`류 도입은 Phase 1 검토, 웹폰트 비용/성능 고려).
- 스케일(모바일): Display 24 / Title 20 / Subtitle 17 / Body 15 / Caption 13. 행간 1.4~1.5.

## 4. 스페이싱 · 레이아웃
- 4px 그리드(4/8/12/16/24/32). 화면 좌우 패딩 16.
- 하단 내비 높이 56 + safe-area. FAB는 우하단(엄지), 지도/탐색에서만 노출.
- 카드 라운드 12, 이미지 라운드 8, 그림자 최소.

## 5. 핵심 컴포넌트
- **SpotCard**: 대표사진 + 이름 + 카테고리칩 + 검증상태 뱃지 + 거리(옵션) + 저장 하트.
- **VerificationBadge**: 색+아이콘+라벨. 4상태.
- **MapPin / Cluster**: 색=검증상태, 아이콘=카테고리, 숫자=클러스터.
- **ExploreToggle**: 피드 ⇄ 지도 세그먼트.
- **SaveSheet**: 원탭 저장 후 컬렉션 선택/생성 바텀시트.
- **CheckinButton**: 상태(가능/반경밖-거리표시/정확도불량/완료).
- **BeforeAfterSlider**: 대표사진 ↔ 내 사진 드래그 비교.
- **BottomNav**: 홈·탐색·컬렉션·프로필.
- **AuthGateModal**: 액션 시 소프트 게이트.

## 6. 모션
- 짧고 기능적으로(150~250ms). 전환은 페이드/슬라이드. 지도 이동은 관성 최소. 배지 획득만 약간의 축하 모션 허용.

## 7. 접근성 체크
- 대비 AA(본문 4.5:1). 터치 타깃 ≥ 44px. 포커스 링 가시. 이미지 대체텍스트. 색+라벨 병기. 모션 축소 설정 존중.

## 8. TODO
- 다크 팔레트 값, 폰트 채택(웹폰트 vs 시스템), 아이콘 세트, 카테고리 아이콘 매핑, 빈/에러 상태 일러스트.
